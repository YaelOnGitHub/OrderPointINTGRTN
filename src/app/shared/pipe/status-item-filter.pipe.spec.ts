import { StatusItemFilterPipe } from './status-item-filter.pipe';

describe('StatusItemFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new StatusItemFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
