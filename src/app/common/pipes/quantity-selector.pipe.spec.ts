import { QuantitySelectorPipe } from './quantity-selector.pipe';

describe('QuantitySelectorPipe', () => {
  it('create an instance', () => {
    const pipe = new QuantitySelectorPipe();
    expect(pipe).toBeTruthy();
  });
});
